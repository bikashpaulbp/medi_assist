// import 'package:get_storage/get_storage.dart';
// import '../constants/app_constants.dart';

// /// Call this in main() BEFORE GetStorage.init()
// /// Initializes both storage boxes so they're ready for use
// Future<void> initAllStorageBoxes() async {
//   // Default box for user data
//   await GetStorage.init();
//   // Settings box — separate, never cleared
//   await GetStorage.init(AppConstants.settingsBoxName);
// }